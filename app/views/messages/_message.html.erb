<div id="message-">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%a %e %b at %l:%M %p") %></i>
  </small>
  <p><%= message.content.html_safe %></p>
</div>
