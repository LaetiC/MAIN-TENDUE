<div id="message-">
  <small>
    <strong><%= message.user.first_name %></strong>
    <i><%= message.created_at.strftime("%a %b %e at %l:%M %p") %></i>
  </small>
  <p><%= message.content %></p>
</div>

<%# class="message-row d-flex <%= message.user == current_user ? 'justify-content-right' : 'justify-content-left' %>

